# Error Handling Improvements Guide

**Коммит:** `TBD` (будет добавлен после commit)

## Обзор

Реализована улучшенная система обработки ошибок при загрузке текущего пользователя (checkAuth). Система различает три типа ошибок:

1. **Ошибки аутентификации (401/403)** - требуют logout
2. **Ошибки сети/сервера (5xx, Network Error)** - сохраняют токены, показывают уведомление
3. **Другие ошибки** - обработка по умолчанию

## Проблема

Предыдущая реализация обрабатывала все ошибки при `getCurrentUser()` одинаково - выполняла logout при любой ошибке. Это создавало следующие проблемы:

- **Временные сбои сервера** → Пользователь выходит из системы
- **Потеря интернета** → Токены удаляются, нельзя воспользоваться кэшем
- **Невозможно восстановиться** → Нужно заново логиниться после восстановления соединения
- **Плохой UX** → Пользователь теряет контекст и данные

## Решение

### 1. Helper функция analyzeAuthError()

Анализирует ошибку и возвращает объект `ErrorAnalysis` с информацией о типе ошибки:

```typescript
interface ErrorAnalysis {
  isAuthenticationInvalid: boolean;  // 401 или 403
  isNetworkError: boolean;           // Нет response или Network Error
  isServerError: boolean;            // 5xx ошибки
  status: number | null;             // HTTP статус код
  message: string;                   // Техническое сообщение
  userMessage: string;               // Сообщение для пользователя
}
```

**Определение типа ошибки:**

```typescript
export function analyzeAuthError(error: any): ErrorAnalysis {
  const status = error?.response?.status;

  // 401 Unauthorized или 403 Forbidden - аутентификация недействительна
  if (status === 401 || status === 403) {
    return {
      isAuthenticationInvalid: true,
      userMessage: status === 401
        ? 'Ваша сессия истекла. Пожалуйста, войдите снова.'
        : 'Ваша учётная запись отключена. Пожалуйста, обратитесь в поддержку.',
    };
  }

  // 5xx ошибки сервера
  if (status && status >= 500) {
    return {
      isServerError: true,
      userMessage: 'Сервер недоступен. Пожалуйста, попробуйте позже.',
    };
  }

  // Ошибки сети (нет response)
  if (!error?.response || error?.message === 'Network Error') {
    return {
      isNetworkError: true,
      userMessage: 'Ошибка соединения с сервером. Проверьте интернет соединение.',
    };
  }

  // Другие ошибки
  return {
    message: 'Произошла ошибка. Пожалуйста, попробуйте снова.',
  };
}
```

**Расположение:** `frontend/services/authApi.ts:225-278`

### 2. Обновлённый AuthContext.checkAuth()

```typescript
useEffect(() => {
  const checkAuth = async () => {
    if (hasToken) {
      try {
        const currentUser = await authApi.getCurrentUser();
        setUser(currentUser);
        setIsAuthenticated(true);
      } catch (err) {
        // Анализируем ошибку
        const errorAnalysis = authApi.analyzeAuthError(err);

        if (errorAnalysis.isAuthenticationInvalid) {
          // 401/403 - аутентификация недействительна, выполняем logout
          await authApi.logout();
          setIsAuthenticated(false);
          setUser(null);
          setError(errorAnalysis.userMessage);
        } else {
          // Ошибка сети или сервера - сохраняем токены, показываем уведомление
          setError(errorAnalysis.userMessage);
          // НЕ очищаем токены!
        }
      }
    }
  };
}, []);
```

**Расположение:** `frontend/contexts/AuthContext.tsx:31-80`

**Поведение:**

- ✓ **401/403**: Выполняет logout, очищает user, показывает ошибку
- ✓ **5xx**: Сохраняет токены, показывает уведомление о недоступности
- ✓ **Network Error**: Сохраняет токены, показывает уведомление о соединении

### 3. Компонент NetworkErrorAlert

Компонент для отображения ошибок сети/сервера, отличных от ошибок аутентификации:

```typescript
export const NetworkErrorAlert: React.FC<NetworkErrorAlertProps> = ({
  type,        // 'network' | 'server' | 'auth'
  message,     // Сообщение об ошибке
  onRetry,     // Callback для повтора
  onClose,     // Callback для закрытия
  visible,     // Видимость алерта
}) => {
  // Отображает алерт в top-right углу с иконкой и кнопками
};
```

**Расположение:** `frontend/components/NetworkErrorAlert.tsx`

**Стили:**
- **network**: Жёлтый (⚠️) - временная ошибка соединения
- **server**: Красный (🔴) - сервер недоступен
- **auth**: Оранжевый (🔒) - требуется переаутентификация

### 4. Unit тесты

Полный набор тестов для helper функции и различных сценариев:

**Файл:** `frontend/services/authApi.test.ts`

**Что тестируется:**

1. **Нормализация ролей** (существующие тесты, сохранены)
2. **Нормализация пользователя** (существующие тесты, сохранены)
3. **Анализ ошибок:**
   - 401 Unauthorized → isAuthenticationInvalid = true
   - 403 Forbidden → isAuthenticationInvalid = true
   - 5xx Server Errors → isServerError = true
   - Network Error → isNetworkError = true
   - Различие между 401 и 403 сообщениями

**Количество тестов:** ~30+ для error analysis

### 5. Storybook stories

Интерактивные примеры для демонстрации компонента:

**Файл:** `frontend/components/NetworkErrorAlert.stories.tsx`

**Stories:**

1. **NetworkError** - ошибка соединения
2. **ServerError** - ошибка 5xx
3. **AuthenticationError** - сессия истекла (401)
4. **AccountDisabledError** - учётная запись отключена (403)
5. **AllErrorTypes** - все типы ошибок с переключением
6. **RealWorldScenario** - реальный сценарий с имитацией checkAuth

## Сценарии использования

### Сценарий 1: Успешная загрузка пользователя

```
App.tsx загружается
  ↓
checkAuth() запускается
  ↓
hasToken = true
  ↓
getCurrentUser() успешно
  ↓
setUser(currentUser) ✓
setIsAuthenticated(true) ✓
user показывается в приложении ✓
```

### Сценарий 2: Сессия истекла (401)

```
App.tsx загружается
  ↓
checkAuth() запускается
  ↓
hasToken = true
  ↓
getCurrentUser() → 401
  ↓
analyzeAuthError() → isAuthenticationInvalid = true
  ↓
logout() - очищаем токены ✓
setIsAuthenticated(false) ✓
setError('Ваша сессия истекла...') ✓
пользователь видит LoginPage ✓
```

### Сценарий 3: Сервер недоступен (503)

```
App.tsx загружается
  ↓
checkAuth() запускается
  ↓
hasToken = true
  ↓
getCurrentUser() → 503
  ↓
analyzeAuthError() → isServerError = true
  ↓
NO logout! Токены остаются! ✓
setError('Сервер недоступен...') ✓
NetworkErrorAlert показывает уведомление ✓
Пользователь может закрыть ошибку или перезагрузить страницу ✓
При восстановлении сервера checkAuth() повторится ✓
```

### Сценарий 4: Потеря интернета

```
App.tsx загружается
  ↓
checkAuth() запускается
  ↓
hasToken = true
  ↓
getCurrentUser() → Network Error
  ↓
analyzeAuthError() → isNetworkError = true
  ↓
NO logout! Токены остаются! ✓
setError('Ошибка соединения...') ✓
NetworkErrorAlert показывает уведомление ✓
Пользователь может:
  - Проверить интернет ✓
  - Клик на Retry для повтора ✓
  - Закрыть уведомление и работать с кэшем ✓
```

### Сценарий 5: Учётная запись отключена (403)

```
Администратор отключает пользователя
  ↓
Пользователь с существующим токеном открывает приложение
  ↓
checkAuth() → getCurrentUser() → 403
  ↓
analyzeAuthError() → isAuthenticationInvalid = true
  ↓
logout() - очищаем токены ✓
setError('Ваша учётная запись отключена...') ✓
Пользователь видит LoginPage и понимает причину ✓
```

## UX Улучшения

### Сообщения пользователю

**Сессия истекла (401):**
```
"Ваша сессия истекла. Пожалуйста, войдите снова."
```
→ Пользователь понимает, что нужно переподтвердить учётные данные

**Учётная запись отключена (403):**
```
"Ваша учётная запись отключена. Пожалуйста, обратитесь в поддержку."
```
→ Пользователь понимает, что нужна помощь поддержки

**Ошибка сервера (5xx):**
```
"Сервер недоступен. Пожалуйста, попробуйте позже."
```
→ Пользователь понимает, что это временная проблема сервера

**Ошибка соединения (Network):**
```
"Ошибка соединения с сервером. Проверьте интернет соединение."
```
→ Пользователь знает, что нужно проверить интернет

### Визуальные индикаторы

**NetworkErrorAlert компонент:**
- Позиция: top-right corner
- Иконка по типу ошибки (⚠️ 🔴 🔒)
- Цветовая схема (жёлтая, красная, оранжевая)
- Кнопка "Повторить" (для network/server ошибок)
- Кнопка "Закрыть" (все типы)
- Кнопка закрытия ✕

## Архитектура

### Поток данных

```
App.tsx
  ↓
AuthProvider (checkAuth effect)
  ├─ getCurrentUser()
  │   ├─ Success → setUser() + setIsAuthenticated(true)
  │   └─ Error → analyzeAuthError()
  │       ├─ isAuthenticationInvalid → logout() + setError()
  │       ├─ isNetworkError → setError() (NO logout)
  │       └─ isServerError → setError() (NO logout)
  │
  └─ useAuth() hook
      ├─ isAuthenticated
      ├─ user
      ├─ error
      └─ clearError()

Components
  ├─ LoginPage (если !isAuthenticated)
  ├─ App content (если isAuthenticated)
  └─ NetworkErrorAlert (если error)
```

### Файлы и изменения

| Файл | Изменения |
|------|-----------|
| `frontend/services/authApi.ts` | Добавлена функция `analyzeAuthError()` и интерфейс `ErrorAnalysis` |
| `frontend/contexts/AuthContext.tsx` | Обновлён `checkAuth()` для условной очистки токенов |
| `frontend/components/NetworkErrorAlert.tsx` | ✨ Новый компонент для отображения ошибок |
| `frontend/services/authApi.test.ts` | ✨ Новый файл с тестами (30+ тестов) |
| `frontend/components/NetworkErrorAlert.stories.tsx` | ✨ Новый файл с Storybook stories |

## Тестирование

### Unit тесты

```bash
cd frontend
npm run test -- authApi.test.ts
```

Проверяет:
- Анализ 401, 403, 5xx, Network Error
- Различие сообщений для разных ошибок
- Правильность структуры ErrorAnalysis

### Storybook

```bash
cd frontend
npm run storybook
```

Открыть: `Components/NetworkErrorAlert`

Тестовые сценарии:
- NetworkError (с Retry)
- ServerError (с Retry)
- AuthenticationError (без Retry)
- AllErrorTypes (интерактивное переключение)
- RealWorldScenario (имитация checkAuth)

### Ручное тестирование

#### Тест 1: Успешная загрузка
```
1. Убедитесь, что Auth сервис доступен
2. Откройте приложение с действительным токеном
3. checkAuth() должен загрузить пользователя ✓
4. Не должно быть ошибок ✓
```

#### Тест 2: 401 (сессия истекла)
```
1. Получите действительный access token
2. Очистите refresh token из localStorage/sessionStorage
3. Дождитесь истечения access token
4. Перезагрузите страницу
5. checkAuth() → 401
6. Должен быть logout ✓
7. Должна быть ошибка в UI ✓
```

#### Тест 3: 403 (учётная запись отключена)
```
1. Логиниться с действительными учётными данными
2. Администратор отключает пользователя (user.enabled = false)
3. Откройте новый tab с приложением
4. checkAuth() → 403
5. Должен быть logout ✓
6. Должна быть ошибка "учётная запись отключена" ✓
```

#### Тест 4: 503 (сервер недоступен)
```
1. Получите действительный токен
2. Остановите Auth сервис (docker-compose down)
3. Перезагрузите приложение
4. checkAuth() → 503
5. Токены должны остаться! ✓
6. Должна быть ошибка "Сервер недоступен" ✓
7. NetworkErrorAlert должен показаться ✓
8. Запустите сервис снова (docker-compose up)
9. Клик на Retry
10. checkAuth() должен успешно загрузить пользователя ✓
```

#### Тест 5: Network Error (отключено интернет)
```
1. Получите действительный токен
2. Отключите интернет (DevTools → Network → Offline)
3. Перезагрузите страницу
4. checkAuth() → Network Error
5. Токены должны остаться! ✓
6. Должна быть ошибка "Ошибка соединения" ✓
7. NetworkErrorAlert должен показаться ✓
8. Включите интернет
9. Клик на Retry
10. checkAuth() должен успешно загрузить пользователя ✓
```

## Интеграция с App.tsx

Для полной реализации, в `App.tsx` нужно добавить NetworkErrorAlert:

```typescript
export const App: React.FC = () => {
  const { error, clearError } = useAuth();

  return (
    <div>
      {/* ... rest of app ... */}
      <NetworkErrorAlert
        type="network" // будет определяться по ошибке
        message={error || ''}
        visible={!!error}
        onClose={clearError}
        onRetry={() => window.location.reload()}
      />
    </div>
  );
};
```

## Производительность

- `analyzeAuthError()`: O(1) - простая проверка свойств
- Без дополнительных сетевых запросов
- Без рекурсии или deep operations
- Безопасно вызывать на каждой ошибке

## Безопасность

- Не раскрываем детали техническихошибок пользователю
- Различие между 401 и 403 для информативности
- Токены сохраняются при сетевых ошибках (можно использовать кэш)
- Токены очищаются только при реальной аутентификации (401/403)

## Будущие улучшения

- [ ] Автоматический retry при Network Error (exponential backoff)
- [ ] Persistent notification в Redux/Zustand
- [ ] Integration with API error interceptors
- [ ] Support for different error messages based on user preferences
- [ ] Automatic check auth every N minutes
- [ ] Token validation before making requests

## Связанные коммиты

- `3c3b530` - Role normalization
- `f300540` - Token refresh logic
- `3850c87` - Disabled user checks
- `TBD` - Error handling improvements (этот коммит)
